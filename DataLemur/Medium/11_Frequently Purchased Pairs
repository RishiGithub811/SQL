--with cte as 
--(
SELECT  t.transaction_id,
t.product_id,
t.user_id,
t.transaction_date,
p.product_name
FROM transactions t 
left join 
products p 
on t.product_id = p.product_id 
--) 
--select transaction_id,
--from cte 
--group by transaction_id
