select user_id,
spend,
transaction_date
from 
(
select user_id,spend,
transaction_date,DENSE_RANK() 
over (partition by user_id 
order by transaction_date) as rnk
from transactions
) t 
where rnk = 3
